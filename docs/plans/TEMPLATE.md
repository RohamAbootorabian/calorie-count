# Plan: <Title>

- **Status**: Draft → In Review → Approved → In Progress → Done
- **Created**: YYYY-MM-DD
- **Plan #**: NNNN

## Problem / Goal
What are we solving and why? What does "done" concretely look like?

## Non-goals
What we are explicitly NOT doing in this task (prevents scope creep).

## Proposed approach
The design. Aim for the **smallest change that fully solves the problem**.

## Files to change
- `path/to/file` — what changes and why

## Data model / schema impact
Tables, columns, migrations, RLS policies, storage buckets. Write "None" if N/A.

## Edge cases & failure modes
Bad/empty/huge input, offline, slow network, partial failures, AI returns
malformed or low-confidence data, auth missing, etc.

## Test / verify plan
How we confirm it works: typecheck, lint, manual run in Expo Go, specific
scenarios to check.

## Rollout
Order of operations: migrations to run, env vars/secrets to set, deploy steps.

## Open questions
Anything unresolved that needs a decision.

---

## Review
<!-- Filled by /review-plan. Findings grouped: BLOCKER / SHOULD-FIX / NIT, each
     with a suggested resolution. Verdict: APPROVED or NEEDS CHANGES. -->

## Execution log
<!-- Filled during execution: what actually happened, any deviation from the plan
     and why, final verification result. -->
