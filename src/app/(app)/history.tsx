/**
 * The History tab route (plan 0012) — a thin re-export of the feature screen.
 * (Replaced the Expo starter "Explore" screen; the route is now `/history`.)
 */
export { default } from '@/features/history/screens/history-screen';
