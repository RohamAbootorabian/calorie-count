/**
 * The Home tab route (plan 0014) — a thin re-export of the dashboard feature screen.
 * (Replaced the Expo starter "Welcome to Expo" screen; the route stays `/`.)
 */
export { default } from '@/features/dashboard/screens/dashboard-screen';
