/**
 * Public typography surface for the design system.
 *
 * `ThemedText` (in src/components) is the canonical implementation — this is just
 * the public name features import. Do not duplicate the implementation here.
 */
export { ThemedText as Text } from '@/components/themed-text';
export type { ThemedTextProps } from '@/components/themed-text';
